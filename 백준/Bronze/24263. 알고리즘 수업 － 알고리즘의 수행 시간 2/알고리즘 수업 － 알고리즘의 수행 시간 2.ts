const input = require('fs').readFileSync('dev/stdin').toString().trim().split('\n');
const n = parseInt(input[0]);

const numOperations = n;
const maxDegree = 1;

console.log(numOperations);
console.log(maxDegree);